server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  location = /healthz {
    access_log off;
    add_header Content-Type text/plain;
    return 200 'ok\n';
  }

  location = /metrics {
    access_log off;
    default_type text/plain;
    try_files /metrics.prometheus =404;
  }

  location / {
    try_files $uri $uri/ /index.html;
  }
}

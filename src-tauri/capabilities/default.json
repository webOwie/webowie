{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability for the webOwie main window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
